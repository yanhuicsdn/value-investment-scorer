# 快速开始指南

## 📦 项目已创建完成！

项目位置：`/Users/yanhui/Downloads/value-investment-scorer`

## 🚀 启动项目

### 方式一：开发模式（已运行）
开发服务器已在后台运行：http://localhost:3005

如需重启：
```bash
cd /Users/yanhui/Downloads/value-investment-scorer
npm run dev
```

### 方式二：生产构建
```bash
npm run build
npm start
```

## 🔐 首次使用

1. **打开浏览器访问**: http://localhost:3005

2. **注册账户**:
   - 输入邮箱
   - 输入密码（任意密码）
   - 点击"登录"
   - 系统会自动为您创建账户

3. **准备CSV数据文件**:
   参考以下格式创建CSV文件：

```csv
公司名称,当前股价,市值,总股本,市盈率(P/E),市净率(P/B),净利润,经营现金流,总资产,总负债,净资产收益率,投入资本,税率,总债务,流动资产,流动负债,现金及现金等价物,扣除非经常性损益后净利润,权益乘数,年度分红,股票回购金额,商誉,无形资产,应收账款
贵州茅台,1680.50,2100000,1250,28.5,9.2,720000,650000,2800000,800000,28.5,1500000,0.25,500000,1500000,600000,800000,700000,1.5,300000,50000,100000,50000,200000
```

4. **上传并评分**:
   - 点击"选择文件"上传CSV
   - 选择"ML动态加权评分"（推荐）
   - 点击"开始评分"

5. **查看结果**:
   - 自动跳转到"评分结果"标签
   - 查看每只股票的评分详情
   - 点击"导出结果"下载CSV

## 📁 项目文件说明

```
value-investment-scorer/
├── src/
│   ├── app/                    # Next.js 页面和API路由
│   │   ├── api/               # API端点
│   │   │   ├── auth/          # 认证API
│   │   │   └── scoring/       # 评分API
│   │   ├── auth/              # 登录页面
│   │   ├── layout.tsx         # 根布局
│   │   ├── page.tsx           # 主页
│   │   └── globals.css        # 全局样式
│   ├── components/            # React组件
│   │   ├── ui/               # shadcn/ui基础组件
│   │   ├── dashboard.tsx     # 主仪表板
│   │   ├── csv-uploader.tsx  # CSV上传组件
│   │   ├── scoring-results.tsx # 评分结果展示
│   │   └── scoring-history.tsx # 历史记录
│   └── lib/                  # 工具库
│       ├── scoring/          # 评分核心逻辑
│       │   ├── types.ts      # 类型定义
│       │   ├── constants.ts  # 常量和权重
│       │   ├── calculations.ts # 评分计算函数
│       │   ├── scorer.ts     # 主评分器
│       │   └── csv-parser.ts # CSV解析器
│       ├── auth.ts           # NextAuth配置
│       ├── prisma.ts         # Prisma客户端
│       └── utils.ts          # 工具函数
├── prisma/
│   ├── schema.prisma         # 数据库模型
│   └── dev.db               # SQLite数据库文件
├── public/                   # 静态资源
├── package.json             # 项目配置
├── tsconfig.json            # TypeScript配置
├── tailwind.config.ts       # Tailwind CSS配置
├── next.config.ts           # Next.js配置
└── README.md               # 项目文档
```

## 🔧 开发相关命令

```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 构建生产版本
npm run build

# 启动生产服务器
npm start

# 运行代码检查
npm run lint

# 数据库相关
npx prisma generate    # 生成Prisma客户端
npx prisma db push     # 推送schema到数据库
npx prisma studio      # 打开数据库管理界面
```

## 📊 CSV文件完整字段列表

### 必需字段（评分必须）
- 公司名称 / company_name
- 当前股价 / stock_price
- 市值 / market_cap
- 总股本 / total_shares
- 市盈率(P/E) / pe_ratio
- 市净率(P/B) / pb_ratio

### 可选字段（提供更准确评分）
- 净利润 / net_income
- 扣除非经常性损益后净利润 / adjusted_net_income
- 息税前利润(EBIT) / ebit
- 投入资本 / invested_capital
- 税率 / tax_rate
- 总债务 / total_debt
- 总负债 / total_liabilities
- 流动资产 / current_assets
- 流动负债 / current_liabilities
- 现金及现金等价物 / cash_equivalents
- 经营现金流 / operating_cash_flow
- 总资产 / total_assets
- 营业收入 / revenue
- 净资产收益率(ROE) / roe
- 权益乘数 / equity_multiplier
- 年度分红 / dividends_paid
- 股票回购金额 / share_repurchases
- 商誉 / goodwill
- 无形资产 / intangible_assets
- 交易性金融资产 / trading_securities
- 应收账款 / accounts_receivable

## 🌐 部署到Vercel

1. **推送到GitHub**:
```bash
cd /Users/yanhui/Downloads/value-investment-scorer
git init
git add .
git commit -m "Initial commit"
# 创建GitHub仓库后：
git remote add origin <your-github-repo-url>
git push -u origin main
```

2. **在Vercel导入**:
   - 访问 https://vercel.com
   - 点击"New Project"
   - 导入GitHub仓库
   - Vercel会自动检测Next.js配置

3. **配置环境变量**（生产环境推荐使用PostgreSQL）:
   - `DATABASE_URL`: PostgreSQL连接字符串
   - `NEXTAUTH_SECRET`: 随机生成的密钥（`openssl rand -base64 32`）
   - `NEXTAUTH_URL`: 你的域名

## 💡 常见问题

**Q: 忘记密码怎么办？**
A: 直接用相同邮箱注册新密码即可覆盖（简化的认证系统）

**Q: CSV上传失败？**
A: 检查文件编码是否为UTF-8，确保包含必需字段

**Q: 评分结果异常？**
A: 某些字段可能缺失或格式不正确，系统会给出0分

**Q: 如何清空历史记录？**
A: 每条记录右侧有删除按钮

## 📚 技术支持

- Next.js文档: https://nextjs.org/docs
- Prisma文档: https://www.prisma.io/docs
- shadcn/ui: https://ui.shadcn.com
- NextAuth.js: https://next-auth.js.org

---

祝您使用愉快！🎉
